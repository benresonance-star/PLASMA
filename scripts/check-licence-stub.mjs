/**
 * G0.1 placeholder for §5B.8 licence inventory CI.
 * G0A.2 will replace this with a real dependency/licence register check.
 */
console.log('[licence:check] stub OK — full register lands in G0A.2');
process.exit(0);
